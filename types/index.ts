export type TLink = {
  name: string;
  href: string;
};

export type TServiceCard = {
  title: string;
  text: string;
  image: any;
  link: string;
};

export type TStep = {
  title: string;
  text: string;
  image: string;
};
